import palette from "./palette";

const themeColors = {
    primary: palette.primary,
    secondary: palette.secondary,
    white: palette.white,
    black: palette.black,
    textHeading: palette.yellow,
    inputBg: palette.grey,
    transparent: palette.transparent,
    placeholderColor: palette.drakGrey,
    focusedPlaceholder: palette.lightGrey,
    lightGrey: palette.lightGrey,
    labelColors: palette.gray,
    error: palette.danger,
    success: palette.green,
    toggleColor: palette.dimgray,
    greyShade: palette.greyShade,
    actionItemColor: palette.darkGrey,
    backDropColor: palette.drakColor,
    tagColor: palette.purpal
};

export default {
    colors: themeColors
};